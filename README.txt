1. Set MAVEN_HOME, JAVA_HOME on your window
2. Clone automation to your window.
3. Open Cmd and navigate to project folder, input "mvn clean install -Dsuite=parallel" and press Enter to see how it works.
4. Open "src/test/java/reports/" to see the report (includes screenshot if failed)